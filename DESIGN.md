---
name: Persona Hub
description: 原创角色投出一张卡片，卡片成为唯一项目的电影式个人作品站
---

<!-- SEED: re-run $impeccable document once there's code to capture the actual tokens and components. -->

# Design System: Persona Hub

## 1. Overview

**Creative North Star: "被掷出的预告函"**

整个视觉系统像一张在黑色舞台上突然被投向镜头的行动预告函。角色、卡片和页面不是三个独立元素，而是同一次动作的三个阶段。首页采用选定概念图 2 的角色构图，卡片接近镜头后进入选定概念图 3 的页面化状态。

画面必须同时具备电影镜头的冲击力和个人作品站的清晰度。日式图形黑色电影、怪盗姿态和漫画式定格是氛围来源，不是可复制的素材库。系统明确拒绝无逻辑的三维物体拼接、杂乱漫画拼贴和现成游戏角色复刻。

**Key Characteristics:**

- 一个角色、一个卡片、一个项目。
- 黑色空间承担舞台，绯红只标记动作，浅色只承担信息与轮廓。
- 斜线和透视都沿卡片运动方向组织，不使用随机装饰。
- 动画经过编排，但正文始终由可访问的 DOM 呈现。
- 移动端保留叙事，不强求桌面端相同的渲染复杂度。

## 2. Colors

采用三角色全色板：深黑是空间，绯红是动作，低彩浅色是信息。具体 OKLCH 数值在第一轮实现中依据选定概念图校准。

### Primary

- **行动绯红**（实现阶段解析）：只用于卡片轨迹、扫描线、主要动作和极少量选中状态。

### Secondary

- **舞台黑**（实现阶段解析）：覆盖绝大部分页面和三维场景，保持中性，不加入紫蓝渐变。

### Neutral

- **印刷浅色**（实现阶段解析）：用于标题、卡片边缘、角色轮廓和正文高对比文本。
- **烟尘灰**（实现阶段解析）：用于次要元数据，不得承担主要正文。

### Named Rules

**The Red Has a Job Rule.** 每一道红色都必须说明动作、选择或状态；没有功能的红色装饰一律删除。

**The Black Stage Rule.** 深黑不是通用暗色模式，而是角色与卡片表演的舞台。背景不得被随机纹理、粒子或面板填满。

## 3. Typography

**Display Font:** 窄体无衬线展示字（实现阶段从非默认字体目录中确定）
**Body Font:** 高可读人文无衬线（实现阶段确定）

**Character:** 标题像印刷海报上的剪切字，正文则保持安静、清晰和正常阅读节奏。字体最多两套，不使用衬线杂志风或技术产品式等宽字堆叠。

### Hierarchy

- **Display**（粗体，流体尺寸，最大 6rem，紧凑行高）：仅用于首页主句和项目标题。
- **Headline**（中粗体，约为正文的 2.4 倍）：用于案例章节标题。
- **Title**（中粗体，约为正文的 1.5 倍）：用于元信息分组和弹层标题。
- **Body**（常规，15px 至 18px，宽松行高）：用于项目叙述，行长限制在 65 至 75 个字符。
- **Label**（中粗体，短词可大写）：只用于导航、按钮和少量状态，不把完整句子写成全大写。

### Named Rules

**The One Shout Rule.** 每个视口只允许一个高声量标题，其余文字必须退后。

## 4. Elevation

界面默认扁平，空间深度来自角色、卡片、镜头和光照。DOM 元素不使用大面积玻璃模糊和软阴影。只有卡片接近镜头时允许一层短暂、方向明确的接触阴影或红色反光。

### Named Rules

**The Physical Depth Rule.** 能由真实三维位置表达的深度，禁止再用装饰性 CSS 阴影重复表达。

## 6. Do's and Don'ts

### Do:

- **Do** 用一次清晰的投卡动作完成主要导航。
- **Do** 让卡片运动方向决定红色切痕、构图斜线和页面揭示方向。
- **Do** 保持首页一个角色、一个卡片、一个行动入口。
- **Do** 让项目正文回归正常滚动、语义结构和高对比阅读。
- **Do** 为低性能设备和减少动态效果设置保留完整内容的替代路径。

### Don't:

- **Don't** 使用“无逻辑的三维物体拼接”或为了展示 WebGL 而堆叠模型。
- **Don't** 制作“杂乱、过密、同时争抢注意力的漫画拼贴”。
- **Don't** 套用“通用太空、旅行、地形、书本或城市主题”。
- **Don't** 直接复制《女神异闻录 5》的现成角色、服装、面具、标志或界面资产。
- **Don't** 把正文和主要导航只放在 Canvas 内。
- **Don't** 使用强制滚动劫持、自动播放声音、渐变文字、玻璃卡片或随机粒子雨。
