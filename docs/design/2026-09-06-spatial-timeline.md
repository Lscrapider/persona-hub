# Spatial Timeline

This is an experience timeline, with the existing latest-first sequence: 2026.07 work, 2025.05 education, 2024.10 preparation, 2022.07 work, 2018.09 education. Both locale JSON files are unchanged. The geometry expresses record categories, never a claim about qualification completion, achievement, or dependencies between jobs.

## Composition and motion

The title remains on the shared page inset. Desktop uses three unboxed reading zones: sticky title/summary, a warm copper solid ribbon, and DOM dates and descriptions. The ribbon passes through attachment saddles on the category models. The five current records have distinct models. Undergraduate study uses six bound beveled leaves that fan open. Java work uses an open guide jig with an insert and locking bridge that seat together. IELTS preparation uses a ruled slide whose cursor aligns to the center. UNSW AI study uses a two-sided hinged research folio with abstract matrix and annotation leaves. AI application work uses a compact product face with three staggered components and retaining arms that seat together. Inactive structures retain only a small angular idle motion. A small volumetric bird now follows reading movement along the ribbon and docks only within the reading-position neighborhood of an artifact. There is no additional moving highlight, sphere or decorative halo.

The camera translates in document coordinates derived from actual article bounds. A ResizeObserver tracks the host, rail and each article, so locale wrapping and viewport changes update geometry, camera and native button projection together. The long ribbon exists as geometry; only a viewport-sized surface is rendered. At 52rem and below the title returns to normal flow and the models reduce to a side strip.

## Ownership and resilience

- `TimelineRail.tsx`: ordered real records; no text is hidden behind interaction or animation.
- `useActiveTimelineRecord.ts`: one reading focus from passive native scroll; no imperative scroll selection or hover owner.
- `TimelineStage.tsx`: near-viewport dynamic import, persisted effect mode, and React lifetime boundary.
- `timelineModels.ts`: original procedural record-specific geometry, category fallbacks, shared materials, operation poses and exact moving contact points.
- `timelineCompanion.ts`: the volumetric bird, feather layers and wing/head/foot articulation.
- `timelineActivity.ts`: the shared reach/manipulate/release envelope for bird and artifact.
- `timelineRenderer.ts`: viewport sizing, projection, bounded animation scheduling, context loss/restoration, and idempotent cleanup including partial initialization and frame failures.
- The global archive renderer no longer owns any Timeline paths, nodes, buffers or uniforms. Timeline retains its section index so neighboring scenes keep their original transitions.

FULL continuously damps toward the active pose and repeats the active artifact’s manipulation after landing. Each inactive artifact retains its last operation phase and damps its operation weight down, so reversing mid-action does not snap a page or key back. STATIC invalidates once on reading/resize/mode or deliberate bird interaction. Hidden and offscreen scenes cancel RAF; unmount or terminal render failure releases geometry, materials, renderer and observers/listeners. Context restoration remeasures and redraws. A one-pixel Canvas 2D swatch only converts CSS colors; it is not a visual renderer, and no textures or external assets are loaded. The backing store is capped at DPR 1.5 and 2048 pixels per dimension.

## Verification evidence and limits

Browser validation used the existing `127.0.0.1:3000` Next.js development preview in the Codex in-app browser. Checked 1440×900, 1920×1080, 2560×1440 and 1600×600, plus 390×844 English reflow. Timeline heading, model strip and article bounds stay separate and within the page; original content remains complete in both languages. Real screenshots were captured outside the repository.

Earlier verification before the interaction refinement covered model clicking and textual selection (both controls have now been removed), Tab focus with a visible outline, forward/reverse native reading, FULL→STATIC→FULL, first-route loading, leaving/re-entering the scene, and final-record access. STATIC frame counters remain unchanged while idle; offscreen counters likewise remain unchanged. Hero/global WebGL reports ready, and Projects and Logs still render their existing scenes. Final browser checks exclude earlier HMR errors emitted while new files were being written, and the CSS Lab warning fixed during development.

Limits: the in-app browser's 2560 screenshot export has a scaling/cropping defect, so its layout check relies on actual 2560 DOM bounds; 1440/1920 and wide-short screenshots are the visual evidence. The browser keeps background tabs `document.hidden=false`, so the hidden-document branch was code-reviewed rather than falsely reported as a successful visibility test. Context-loss and exceptional allocation paths were reviewed, not GPU fault-injected. The extra 390-wide whole-page check found a pre-existing Projects overflow (391px document width); Timeline itself stays inside its 375px content viewport. Projects was left outside this change's scope.

Typecheck, repository lint and production build passed. The premium static audit in strict mode returned zero findings. Lint has 136 existing warnings outside the changed files. The environment uses Node 24.16 although package engines requests Node 22; the build also reports an existing content-loader/Turbopack tracing warning. No unit tests were created and no git commit, push or branch operation was performed.


## Earlier model and highlight refinement

The standalone gold sleeve was removed after visual feedback that it read as a separate object traveling on the timeline. That intermediate signal belonged to the ribbon material through a stable reading-position uniform; its soft ends follow the continuous surface and create no extra geometry or animation loop. The two repeated study/work models were differentiated by stable record IDs, independent of locale. Existing book, Java jig and calibration actions remain, while the folio opens on its spine and the application components seat in a short overlapping sequence. All poses still derive from the same interruptible active amount.

Follow-up validation: 1440×900 and 1600×600 browser inspection covered all five model poses, keyboard selection and FULL/STATIC. Idle STATIC frame counts remained stable. No browser shader errors or warnings were reported. Typecheck, feature ESLint and production build passed. No new tests or git write operations were performed.


## Reading companion and continuing actions

The subsequent direction replaces the abstract reading highlight with a bird that accompanies the visitor. Native scroll drives continuous flight near the existing ribbon. Proximity to a record controls docking; stopping between records does not attract the bird to either node. Native scrolling owns record focus. The companion neither scrolls the document nor creates extra records.

A continuous category-paced work cycle runs only in FULL after landing: undergraduate pages turn and return, the research folio lifts an independent annotation page, the calibration slide scans and recenters, the Java model receives a physical Enter press, and the application cycles through three component keys. The bird has a real beak-tip marker and each model exposes its moving contact in local coordinates. World transforms align them, with a reach plateau covering the actual operation. The bird's natural observation, head/neck and foot motions continue between operations. Flight unfolds and articulates the wings; landing folds them.

The bird’s intended appearance is anatomical and volumetric, with natural brown matte feathers and a pale breast. No external bird model, texture or animation is used; body surfaces, feather geometry and joint poses are original procedural work. This remains an artistic reading companion, not an ornithological simulation. STATIC shows a stationary bird and resolved artifact pose; hidden/offscreen behavior, bounded canvas size, semantic DOM and resource disposal remain unchanged.


Companion refinement verification: the planar copper prototype was replaced with a natural wren-like volumetric model (continuous anatomical surfaces, curved layered feathers, shoulder/elbow/wrist wings, articulated legs and three forward/one rear toe). The current renderer uses 106 bird meshes after the frontal/back feather refinement, no external textures. Wing motion has a brisk 38% power stroke and a 62% folded recovery, with a brief landing compression/grip and occasional feather settling while perched. This is a procedural artistic model rather than a scanned photoreal asset.

The companion was inspected in the browser at 1440×900, 1920×1080 and 1600×600. Verified ongoing study/slider/Enter/application actions after scroll stops, natural feather/body appearance, facing changes, earlier keyboard year selection (since removed), offscreen pause/re-entry and FULL/STATIC. Static frame and cycle counters remained unchanged between observations. The same-year cycle counter advanced across multiple complete manipulations in FULL; no independent bird RAF was introduced. Final browser checks reported no console warnings/errors. Typecheck and Timeline ESLint passed; production build retained the existing repository Node-version and content-tracing warnings. Existing hidden-tab and GPU fault-injection validation limits above still apply.


## Interaction and continuity refinement

The old 160ms “scroll ended” position switch was removed. Continuous reading position determines the travel target in both moving and idle frames. A node enters docking inside a bounded radius and releases at a larger radius; the docking weight is already zero at the entry boundary, so reversing or stopping in a gap cannot alternate between two destinations. A fully docked bird continues the category-paced semantic work cycle. Old artifact actions retain their phase while their weights fade out.

The same viewport-sized canvas now spans the full inner width. A separate DOM model-zone measurement preserves existing model positions and scale. The bird can make a 3.6-second click/Enter/Space response through the whitespace below the heading; the route clears the heading before crossing left and clears its side before returning upward. Native scroll cancels the response. Mouse position only affects gaze. Front, three-quarter and back orientations use quaternion blending; surface-normal approaches keep the body in front of each artifact while its beak meets the moving contact. A slightly smaller maximum bird scale keeps undergraduate page contact clear of the heading safety margin.

All artifact and textual jump buttons are removed. Only the bird retains a projected native hit target, localized through LocaleProvider. Hover draws no box; keyboard focus retains a dashed circular indicator. Offscreen targets are hidden and removed from tab order. STATIC interaction toggles a finite pose with one redraw; FULL resumes the shared renderer loop. No second canvas, RAF, React frame-state or external model asset was added.

Latest browser verification used 1440×900, 1920×1080, 2560×1440 and 1600×600. It covered stable between-year hovering, reversed scrolling, continuing calibration/book operations, the click excursion and native keyboard response, model clicks without navigation, pointer gaze, STATIC idle counters and STATIC click pose. At 2560 the buffer measured 2048×1228; title, model-zone and article bounds stayed separate with no horizontal overflow. Actual screenshots are in the task’s `timeline/` visualization directory: `interaction-1440-zh.png`, `interaction-1920-zh.png`, `interaction-book-1920.png`, `interaction-1600x600-zh.png`, and `interaction-static.png`. Screenshots show individual poses; the movement checks also use observable frame, position, pose and cycle diagnostics. The procedural bird has anatomical volume and articulated feathers, but remains an artistic model rather than a scanned photorealistic bird.

Final follow-up: English 1440×900 contains all five original experiences and the localized bird control; 390×844 reflow keeps Timeline within its 375px content viewport (359px article right edge). Fully offscreen, the frame counter stayed at 30865 across observations and the bird button had tabIndex -1 / visibility hidden; re-entry and locale remount resumed rendering. STATIC stayed at frame 10, clicking advanced it once to 11, and idle observations remained at 11. Mouse click and keyboard Return produced greeting states without an application-driven scroll. Clean browser console checks reported no warnings/errors. Final typecheck, scoped ESLint, full repository lint and production build succeeded, with the existing repository warnings noted above. Added English evidence: `interaction-1440-en.png`.


## Final anatomical and handling polish

The head inspects the corner before the torso turns toward it. The lower mandible has a real pivot at its root: light opening during approach, closure before the pull, and reopening after the leaf settles. The torso remains in an oblique working orientation between operations instead of swiveling 180 degrees each cycle. Position stays damped; a maximum .115-unit neck extension compensates for the torso’s short follow-through delay while holding the page. The page action now lasts 7.2 seconds, with distinct preparation, grip, pull, pause, settle and release intervals. Calibration uses 6.1 seconds; work keys use 5.4 seconds. All gestures remain interruptible and event-driven in STATIC.

Airborne legs fold at both joints and tuck toes beneath the belly; hip joints now inherit the torso transform. Wing feather thickness is reduced, primaries use a tapered length distribution and lighter feather tones are less saturated. The upper beak marker remains the contact reference.

The undergraduate front leaf and research annotation leaf have shallow continuous curvature, including their embossed marks. Corner tension begins before hinge rotation and relaxes later. Original vertices/normals are cached, surfaces are subdivided once, and dynamic position plus analytic normal updates occur only when bend changes. These add 7,798 / 8,476 dynamic vertices respectively, with about 183 / 199 KiB uploaded per changing pose. Node-only sampling measured about .055 / .058 ms per model animation call; this is not a browser GPU performance claim. The contact point uses exactly the same deformation as the page surface.

The browser inspection of undergraduate handling covered multiple complete cycles; sampled closed-grip contact error rounded to .0000 scene units. Final evidence: `polished-bird-book-1920.png`. Render failure now explicitly hides the canvas so a failed graphics context cannot cover the readable DOM with an opaque surface.

Final polish follow-up: the research folio was checked across several complete cycles as well; after tuning the damped torso response during grip, sampled contact error also rounded to .0000. At 1440×900, STATIC held at frame 13565, clicking produced frame 13566 and subsequent idle observation stayed there. The local preview process was restored after it exited; the recovered preview loaded the final scene, click greeting left scrollY unchanged, subsequent native scrolling interrupted the greeting, and its console reported no warnings/errors. Title stacking is explicitly above the transparent canvas. `polished-bird-folio-1920.png` records the latest paper/feather rendering. Final typecheck, Timeline ESLint and production build passed.


## Click excursion stability fix

A user-reported between-year click stutter exposed two distinct controls fighting the intended motion: sinusoidal whole-body yaw swung across both directions during one greeting, and per-frame heading avoidance clamped an evolving target using the bird's current position. Captured browser samples showed a stalled lateral path followed by a sudden larger shift. A separate docked recovery feedback used `working` to move the contact goal while also switching `working` off at a hard distance threshold from that goal.

Click now creates a single, fixed world-space route with depth clearance, a rounded corner below the heading, 1.5 seconds outbound, .6 seconds turning, and 1.5 seconds returning. The route owns translation for its complete lifetime. Facing blends from the initial quaternion through one outward-facing pose and one inward-facing pose. Repeated clicks/Return during the route do not restart it. Native scrolling, resize (including window changes without a canvas size change), and STATIC cancel the route. Work eligibility depends on docking and interaction state, not on distance to a work-dependent goal. A docked return ends at the safe perch and restarts observation before touching a page again, avoiding a second retreat from a stale contact point.

Browser regression covered 1440×900, 1920×1080, 2560×1440, and 1600×600, Chinese and English, between-year click trajectories, a held-page click through its full return and next observation/reach, repeated keyboard Return, reverse-scroll cancellation, resize during a greeting, and STATIC's single redraw. Continuous DOM diagnostic samples supplement screenshots: `bird-click-motion-samples.json` and `bird-click-held-page-samples.json` in the task visualization directory. Between-year samples retained constant scrollY, traveled outward once and returned once; held-page samples returned to the perch before starting a fresh observation cycle. No horizontal overflow; the 2560 canvas remained 2048×1228. Clean English browser console: no errors or warnings. Evidence screenshots: `bird-click-fixed-1440.png`, `bird-click-fixed-1600x600.png`. Typecheck, scoped ESLint and production build passed; full lint retained 136 pre-existing warnings and zero errors. No unit tests or Git commits/branches were created. This focused fix does not extend the earlier background-tab/GPU-loss validation claims; screenshots alone are not proof of temporal continuity.
